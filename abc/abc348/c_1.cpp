#include <bits/stdc++.h>
using namespace std;
typedef long long Int;

int main() {
   ios::sync_with_stdio(false);
   cin.tie(nullptr);
   // using AC=tuple<int,int>
   // vector<AC> beans;
   // for(int i=0;i<n;i++){
   //  Int a,c; cin >> a >> c;
   //  beans<0>(i)=a; beans<1>(i)=c;
   // }
   // auto cmp = [](AC x, AC y) -> bool {
   //  return get<0>(x) < get<0>(y);
   //  };
   
   // sort(beans.begin(),beans.end(),cmp);
   // vector<int> colors;
   // Int cand_min;
   // for(AC ac:beans){
   //  cout << get<0>(ac) << ' ' << get<1>(ac) << endl;
   // }

   
   return 0;
}