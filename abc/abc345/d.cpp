#include <bits/stdc++.h>
using namespace std;
 
int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int N,H,W; cin >> N >> H >> W;
  vector<vector<int>> tile(N);
  for(int i=0; i<N; i++){
    for(int j=0; j<2; j++){
        
    }
  }

  return 0;
}