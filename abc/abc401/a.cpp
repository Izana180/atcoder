#include <bits/stdc++.h>
using namespace std;
typedef long long Int;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int s; cin >> s;
    if(200<=s&&s<=299){
        cout << "Success" << endl;
        return 0;
    }
    else
        cout << "Failure" << endl;
    
    return 0;
}
