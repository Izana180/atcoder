#include <bits/stdc++.h>
using namespace std;

int main() {
   ios::sync_with_stdio(false);
   cin.tie(nullptr);
   int h,w; cin >> h >> w;
   vector<vector<char>> grid(h,vector<char>(w,'.'));
   
   int n; cin >> n;
   int count = 0;
   int arg = 0;
   while(count<n){
      
   }
   
   return 0;
}